class PagesController < ApplicationController
  def home
    #@locat = params[:place]
    #if user_signed_in?
      #if @locat == "home"
      #else
    #    @user = User.find(current_user.id)
    #    if @user.paid?
    #      #if @once == "1"
          #  redirect_to profile_path(id: 78)
          #else
          #  redirect_to profile_path(id: 88)
          #end
    #      redirect_to profile_path(id: 88)
    #    else
    #        redirect_to profile_path(id: current_user.id)
    #    end
      #end
    #end
  end
  def about
  end
end